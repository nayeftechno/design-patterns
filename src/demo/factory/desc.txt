In Factory pattern, We use factory functions to create a new objects.
Factory functions, are functions that used to create a new objects with use the new keyword.

*****************************************************

pros
1 - create multiple smaller objects that share the same properties.
2 - more simblier than classes.

cons
1 - it's more memory efficiency to create a new class instance,
    rather than a new factory object.
